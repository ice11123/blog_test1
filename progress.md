# 维护日志

## 2026-08-13

- 建立持久化任务计划和研究记录。
- 完成截图与代码现状核对。
- 确认首页三组双栏配对与分级本地预览方案。
- 第一次管理台补丁因终端乱码文本与 UTF-8 源文件不匹配而失败；改用 UTF-8 结构锚点后成功。
- 管理台加入四步操作说明，编辑区改为拉伸等高布局。
- 新增本地安全预览模块：KaTeX、Mermaid、GitHub Alert、Prism 代码高亮、Spoiler 与 Plot3D 安全适配。
- 预览防抖调整为 800ms，并增加预览状态时间。
- 首页调整为三组语义双栏，900px 以下自动单列。
- 本地浏览器检查确认管理台说明、等高、KaTeX、Mermaid、Plot3D、Spoiler 生效；发现代码块被错误截断，已定位为 import/export 正则跨行问题并修复。
- 首页实际截图显示双栏仍被左右侧栏挤压；新增首页专用 `fullWidth` 布局，首页隐藏侧栏，文章页维持原三栏。
- 最终自动检查通过：`pnpm run check` 0 错误，`pnpm run build` 成功，Worker 测试 20/20，Worker 语法检查成功。
- 最终浏览器检查：管理台顶部等高；目录、编辑列和预览列高度均为 1377px；KaTeX 和 Mermaid SVG 正常；代码块完整；Plot3D/Spoiler 适配存在。
- 首页 1280px 实测内容宽约 1209px，双栏约 578/578，第三组约 665/492；页面无横向溢出。
- 首轮 `pnpm run check` 发现 Prism 缺少类型声明，以及 `marked.Renderer` 类型引用错误；进入针对性修复。
- 用户反馈首页排版不美观、侧栏缺失、字号不合理。本轮开始撤销首页 `fullWidth`，以原作者 18px 正文、13.6px 侧栏、220px 侧栏和约 741px 中心栏为视觉基准返工。
- 已恢复首页左右侧栏并取消 `fullWidth` 隐藏逻辑；首页中心区回到原站约 741px 宽度。
- 首页改为中心栏内有边界的三组双栏卡片，字号、内边距和文章卡片密度按原作者计算样式收敛。
- 月度数据只有 1～2 个点时改用紧凑统计卡，避免空折线图造成大面积留白。
- 截图复核发现近期文章内部 main 宽度泄漏，已局部重置，避免文章卡撑出右侧面板。
- 移除双栏面板错误的 `height: 100%` 约束，修复边框跨行和下半区高度越界。
- 初始化 Beads 项目任务系统并创建/认领 `blog_test1-xmf`，后续维护记录同时写入 Beads 与仓库日志。
- 复核最终本地首页：左右侧栏各 220px，中心正文约 741px，body 18px，侧栏 13.6px；三组双栏卡片上下边界分别完全对齐，页面无横向溢出。
- 实际截图确认欢迎统计、月度统计、两组语言图、贡献墙与近期文章均位于统一主题边框中；近期文章不再溢出右侧面板。
- `pnpm run check` 通过（0 errors / 0 warnings / 0 hints），`pnpm run build` 成功。
- Beads 任务 `blog_test1-xmf` 已关闭；首次 `bd dolt push` 因当前仓库绝对路径过长，Dolt 的本地 Git 缓存触发 Windows `Filename too long`，进入短路径同步处理。
- 提交 `8fd6ee3` 完成首页视觉返工，提交 `d2aee0c` 写入 Beads 状态事件，均已推送到 `origin/main`。
- GitHub Pages 工作流 #39 对 `d2aee0c` 构建成功；线上绕过浏览器旧缓存后复核：左右侧栏各 220px、中心约 741px、正文 18px、侧栏 13.6px，三组卡片上下对齐且无页面横向溢出。
- 线上截图确认浅色主题下左侧目录、右侧文章目录与简介清晰可见，首页卡片边界、标题层级和原作者三栏视觉骨架一致。
- 创建并认领 P0 Beads 缺陷 `blog_test1-r48`；浏览器复现确认行内与块级公式均错位，KaTeX 样式表存在但生成节点的内联 style 被净化器全部删除。
- 修复预览净化顺序：普通 Markdown/MDX 继续禁止 style，仅在净化完成后回填本地 KaTeX 生成的可信 HTML；公式节点增加独立换行规则。
- 浏览器截图复核确认行内欧拉公式、积分上下限、指数和根号均正常显示；块级公式高度由错误的约 39px 恢复为约 65px，KaTeX 定位 style 从 0 个恢复为 39 个。
- 实际修改正文加入分式与求和公式，800ms 后预览自动更新且复杂公式生成 26 个定位 style；同时插入的普通 HTML `style` 被正确移除，安全边界未放宽。
- 提交 `810f767` 已推送，GitHub Pages 工作流 #41 成功。线上管理台带版本参数复核得到 39 个 KaTeX 定位 style，块级公式高度约 65px；线上截图确认欧拉公式、积分上下限、指数与根号均正常。
- 创建并认领 P0 Beads 缺陷 `blog_test1-ef0`；浏览器确认 Spoiler 正文位于默认关闭的 details 内，Prism token 节点存在但计算颜色全部等于正文色。
- 修复后本地浏览器验证：Spoiler 默认展开并显示正文，代码 token 已按主题变量着色（关键字紫、函数黄、类型蓝、标点青）；KaTeX、Mermaid 与 Plot3D 未出现回归。
- 提交 `8bf1055` 已推送并上线。线上管理台验证：Spoiler `open=true` 且正文完整；浅色主题 Prism token 呈现紫色关键字、棕色函数、蓝色类型和深灰标点，不再全部继承正文色。
- 创建并认领 Beads 缺陷 `blog_test1-d24`；桌面实测预览和三列 Grid 高度均为约 1458px，中间 textarea 仅 530px，表单被预览内容拉伸后在授权提示下方留下约 300px 空白。
- 将桌面端目录、编辑列和预览统一限定为 1460px 工作区；预览改为固定外框与内部滚动，正文 textarea 使用 flex 填充表单剩余高度；1180px 与 760px 断点分别使用 760px 和 `min(680px, 78dvh)` 预览高度。
- 浏览器压力测试临时追加 80 个 Markdown 段落：预览正文 `scrollHeight` 从约 1398px 增至 11894px，页面总高度保持 2318px；测试后已恢复原文。桌面及 390×844 移动端截图确认三列边界、提示底部留白和响应式滚动均正常。
- 质量门禁通过：`pnpm run check`、`pnpm run build`、Worker 20 项测试及 `node --check worker/src/index.js` 全部成功。
- 创建并认领 Beads 缺陷 `blog_test1-z1i`；新增预览专用连续空行保留管线，仅在围栏代码块外提取额外空行，不改变草稿与正式发布 Markdown。
- 浏览器实测 4 个连续换行生成 2 个额外预览空白行（约 61px）；代码块内部空行保持原样，块级 KaTeX 继续正常渲染；测试正文已恢复且未保存到本地草稿。
