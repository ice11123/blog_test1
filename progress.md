# 维护日志

## 2026-08-15｜本地仓库迁移到统一博客目录

- 将博客仓库从旧的 Codex 工作目录迁移到 `D:\A Study files\AI_files\blog\blog_test1`，保留原 `.git`、完整提交历史、`main` 分支和 `origin` 配置。
- 首次跨盘 `Move-Item` 被 `node_modules` 深层路径打断，形成 `.git` 和根文件已到目标目录、`src/public/scripts/worker` 暂留源目录的中间状态；随后逐目录合并剩余源码，目标仓库恢复为干净状态。
- 目标仓库共核对 131 个 Git 跟踪文件，中文路径与动态路由文件均存在；`git status --porcelain` 为空。
- 在新目录重新执行依赖安装并运行 `pnpm run check`，结果为 0 errors、0 warnings、0 hints。
- `git fetch origin` 后确认本地 `HEAD` 与 `origin/main` 均为 `1052243bd2a8d8cb9a2cdcd8ee839f9ad1849a00`，远程地址仍为 `https://github.com/ice11123/blog_test1.git`。
- 旧路径现在不再是 Git 仓库，仅剩未跟踪的生成依赖 `node_modules`；自动递归删除被执行环境策略阻止，不影响新目录和云端一致性。

## 2026-08-15｜补全近期迭代记录并固化日志流程

- 审计最近 20 个 Git 提交与三类记录文件，确认代码实现没有未记录的功能分支，但部分“记录型提交”没有在正文中显式关联提交哈希。
- 补齐以下提交链路：`fe45955`（公共状态网络诊断与降级测试）、`0059569`（公共状态刷新速度验证）、`5b6c532`（首页部署记录）、`125d172`（线上首页验收）、`86f018a`（Beads 完成状态）、`e7553fa`（线上截图验收日志）、`5052261`（桌面/移动端/贡献墙截图归档）。
- 复核 `5e03bf5` 首页布局实现和 `609907a` Beads 关闭记录，确认当前 `main`、`origin/main` 一致，工作区无代码改动。
- 发现当前会话初始目录 `D:\A Study files\AI_files\blog` 为空，真实仓库为 `C:\Users\27995\Documents\Codex\2026-08-11\https-user1-cloud-github-io-blog\work\blog_test1`；本次已在真实仓库补记，避免日志落到错误目录。
- 以后每轮固定执行：计划先写 `task_plan.md`；排查结论即时写 `findings.md`；每个阶段结束写 `progress.md`；涉及用户可见功能时同步 `src/content/maintenance.md`；最后记录测试结果、提交哈希和部署状态。

## 2026-08-14｜首页统一仪表盘重构

- 已完成统一首页网格、欢迎统计居中、近期文章 3 篇、状态/公告等高、贡献墙置底与维护公告更新。
- 已通过 1920×1080、1680×1050、1440×900、1280×800、390×844、360×800 断点指标复核；补修移动端右侧栏首屏闪现遮挡问题，等待最终质量门禁。
- 复核发现并修复完整刷新时统计卡渐入观察器未初始化的问题，重新截图确认首屏内容可见。
- 为首页核心统计补充局部可见性兜底，避免任何观察器时序导致空白。
- 提交 `11d6850` 已推送到 `main`；Pages 工作流 `31795982957` 在线显示 Success，线上首页返回 200，确认月度统计已移除、近期文章/维护公告/贡献墙均存在且左右侧栏恢复。
- 创建并认领 Beads 任务 `blog_test1-694`。
- 完成当前 DOM、容器断点、状态卡截断规则、近期文章和贡献墙结构核对；确定采用同一网格的七个直接子模块。
- 用户确认近期文章显示最新 3 篇，贡献墙横跨整个首页主体并位于页面最底部。
- 完成首页七模块统一网格结构、月度统计移除、近期文章前移、贡献墙置底以及欢迎统计卡居中样式；状态卡首次补丁因上下文不匹配未应用，读取真实组件后已改用精确规则。
- 首轮 1680×1050 截图确认两行模块等高、状态文字完整换行且无页面横向溢出；发现并修复公告列表轨道被压缩导致标题不可见的问题。

## 2026-08-14｜首页状态与公告栏并行布局优化

- 复现确认首页 `PublicStatus` 的局部规则将公共两列网格覆盖为单列，导致四张状态卡纵向堆叠。
- 将首页状态卡恢复为桌面端两列两行，539px 以下回落单列；状态卡、状态网格和状态容器补充 `min-width: 0` 与 `box-sizing: border-box`。
- 宽屏信息栏从 280px 调整为 320px，1120px 以上调整为 380px；首页抵消正文内边距后保持中心主内容 760px，并确保状态栏与公告栏同宽同边界。
- 浏览器实测：1680×1050 状态卡为 2×2、状态/公告面板均 320px；1920×1080 信息栏为 380px；1280×800 信息区下移且状态卡为 2×2；390×844 状态卡回落单列，无横向溢出。
- 提交 `ee316a5` 已推送到 `main`；GitHub Pages 工作流 `31788591023` 已成功，线上首页返回 200 且包含系统状态与维护公告模块。

## 2026-08-13

- 建立持久化任务计划和研究记录。
- 完成截图与代码现状核对。
- 确认首页三组双栏配对与分级本地预览方案。
- 第一次管理台补丁因终端乱码文本与 UTF-8 源文件不匹配而失败；改用 UTF-8 结构锚点后成功。
- 管理台加入四步操作说明，编辑区改为拉伸等高布局。
- 新增本地安全预览模块：KaTeX、Mermaid、GitHub Alert、Prism 代码高亮、Spoiler 与 Plot3D 安全适配。
- 预览防抖调整为 800ms，并增加预览状态时间。
- 首页调整为三组语义双栏，900px 以下自动单列。
- 本地浏览器检查确认管理台说明、等高、KaTeX、Mermaid、Plot3D、Spoiler 生效；发现代码块被错误截断，已定位为 import/export 正则跨行问题并修复。
- 首页实际截图显示双栏仍被左右侧栏挤压；新增首页专用 `fullWidth` 布局，首页隐藏侧栏，文章页维持原三栏。
- 最终自动检查通过：`pnpm run check` 0 错误，`pnpm run build` 成功，Worker 测试 20/20，Worker 语法检查成功。
- 最终浏览器检查：管理台顶部等高；目录、编辑列和预览列高度均为 1377px；KaTeX 和 Mermaid SVG 正常；代码块完整；Plot3D/Spoiler 适配存在。
- 首页 1280px 实测内容宽约 1209px，双栏约 578/578，第三组约 665/492；页面无横向溢出。
- 首轮 `pnpm run check` 发现 Prism 缺少类型声明，以及 `marked.Renderer` 类型引用错误；进入针对性修复。
- 用户反馈首页排版不美观、侧栏缺失、字号不合理。本轮开始撤销首页 `fullWidth`，以原作者 18px 正文、13.6px 侧栏、220px 侧栏和约 741px 中心栏为视觉基准返工。
- 已恢复首页左右侧栏并取消 `fullWidth` 隐藏逻辑；首页中心区回到原站约 741px 宽度。
- 首页改为中心栏内有边界的三组双栏卡片，字号、内边距和文章卡片密度按原作者计算样式收敛。
- 月度数据只有 1～2 个点时改用紧凑统计卡，避免空折线图造成大面积留白。
- 截图复核发现近期文章内部 main 宽度泄漏，已局部重置，避免文章卡撑出右侧面板。
- 移除双栏面板错误的 `height: 100%` 约束，修复边框跨行和下半区高度越界。
- 初始化 Beads 项目任务系统并创建/认领 `blog_test1-xmf`，后续维护记录同时写入 Beads 与仓库日志。
- 复核最终本地首页：左右侧栏各 220px，中心正文约 741px，body 18px，侧栏 13.6px；三组双栏卡片上下边界分别完全对齐，页面无横向溢出。
- 实际截图确认欢迎统计、月度统计、两组语言图、贡献墙与近期文章均位于统一主题边框中；近期文章不再溢出右侧面板。
- `pnpm run check` 通过（0 errors / 0 warnings / 0 hints），`pnpm run build` 成功。
- Beads 任务 `blog_test1-xmf` 已关闭；首次 `bd dolt push` 因当前仓库绝对路径过长，Dolt 的本地 Git 缓存触发 Windows `Filename too long`，进入短路径同步处理。
- 提交 `8fd6ee3` 完成首页视觉返工，提交 `d2aee0c` 写入 Beads 状态事件，均已推送到 `origin/main`。
- GitHub Pages 工作流 #39 对 `d2aee0c` 构建成功；线上绕过浏览器旧缓存后复核：左右侧栏各 220px、中心约 741px、正文 18px、侧栏 13.6px，三组卡片上下对齐且无页面横向溢出。
- 线上截图确认浅色主题下左侧目录、右侧文章目录与简介清晰可见，首页卡片边界、标题层级和原作者三栏视觉骨架一致。
- 创建并认领 P0 Beads 缺陷 `blog_test1-r48`；浏览器复现确认行内与块级公式均错位，KaTeX 样式表存在但生成节点的内联 style 被净化器全部删除。
- 修复预览净化顺序：普通 Markdown/MDX 继续禁止 style，仅在净化完成后回填本地 KaTeX 生成的可信 HTML；公式节点增加独立换行规则。
- 浏览器截图复核确认行内欧拉公式、积分上下限、指数和根号均正常显示；块级公式高度由错误的约 39px 恢复为约 65px，KaTeX 定位 style 从 0 个恢复为 39 个。
- 实际修改正文加入分式与求和公式，800ms 后预览自动更新且复杂公式生成 26 个定位 style；同时插入的普通 HTML `style` 被正确移除，安全边界未放宽。
- 提交 `810f767` 已推送，GitHub Pages 工作流 #41 成功。线上管理台带版本参数复核得到 39 个 KaTeX 定位 style，块级公式高度约 65px；线上截图确认欧拉公式、积分上下限、指数与根号均正常。
- 创建并认领 P0 Beads 缺陷 `blog_test1-ef0`；浏览器确认 Spoiler 正文位于默认关闭的 details 内，Prism token 节点存在但计算颜色全部等于正文色。
- 修复后本地浏览器验证：Spoiler 默认展开并显示正文，代码 token 已按主题变量着色（关键字紫、函数黄、类型蓝、标点青）；KaTeX、Mermaid 与 Plot3D 未出现回归。
- 提交 `8bf1055` 已推送并上线。线上管理台验证：Spoiler `open=true` 且正文完整；浅色主题 Prism token 呈现紫色关键字、棕色函数、蓝色类型和深灰标点，不再全部继承正文色。
- 创建并认领 Beads 缺陷 `blog_test1-d24`；桌面实测预览和三列 Grid 高度均为约 1458px，中间 textarea 仅 530px，表单被预览内容拉伸后在授权提示下方留下约 300px 空白。
- 将桌面端目录、编辑列和预览统一限定为 1460px 工作区；预览改为固定外框与内部滚动，正文 textarea 使用 flex 填充表单剩余高度；1180px 与 760px 断点分别使用 760px 和 `min(680px, 78dvh)` 预览高度。
- 浏览器压力测试临时追加 80 个 Markdown 段落：预览正文 `scrollHeight` 从约 1398px 增至 11894px，页面总高度保持 2318px；测试后已恢复原文。桌面及 390×844 移动端截图确认三列边界、提示底部留白和响应式滚动均正常。
- 质量门禁通过：`pnpm run check`、`pnpm run build`、Worker 20 项测试及 `node --check worker/src/index.js` 全部成功。
- 创建并认领 Beads 缺陷 `blog_test1-z1i`；新增预览专用连续空行保留管线，仅在围栏代码块外提取额外空行，不改变草稿与正式发布 Markdown。
- 浏览器实测 4 个连续换行生成 2 个额外预览空白行（约 61px）；代码块内部空行保持原样，块级 KaTeX 继续正常渲染；测试正文已恢复且未保存到本地草稿。
- 创建并认领 Beads 功能任务 `blog_test1-4k3`，开始主页第三信息栏、公共状态、维护公告与顶栏工具迁移。
- 新增 Worker `/api/public-status`，覆盖合法/非法 Origin、匿名访问、90 秒缓存、旧缓存降级和无缓存失败；Worker 测试增至 25 项并全部通过。
- 新增共享状态卡样式、首页公共状态组件与构建期维护公告解析；搜索和六套主题切换器从左侧栏迁移到站名右侧，移动端使用独立工具行。
- 首次生产构建发现维护公告运行时路径失效，改为 `maintenance.md?raw` 构建期导入后恢复成功构建。
- 浏览器断点复核发现移动端继承桌面右侧栏展开状态并覆盖正文，且左侧隐藏抽屉造成约 5px 横向溢出；已修复为移动端初始强制收起右侧栏、左侧抽屉使用完整位移。
- 最终本地验收：1920/1680 第三栏出现，1440/1280 状态公告下移，390/360 单列；最新公告默认展开，其余折叠且框内滚动；搜索弹窗、主题菜单与主题持久化正常。
- 最终质量门禁通过：Astro 检查 0 错误/0 警告，生产构建成功，Worker 25/25 测试通过，Worker 语法检查和 `git diff --check` 通过。
- Cloudflare Worker 已部署版本 `544fedc9-cf23-4a60-a793-678af3ca8f28`；本机到 `workers.dev` 直连 TCP 超时，待站点上线后由合法网页 Origin 触发接口并完成最终在线复核。
- Pages 工作流 #31713330876 对提交 `387b861` 构建成功；线上布局、顶栏、公告折叠和断点均正常，但页面同样无法连接 `workers.dev`。
- 为避免 Worker 网络受限时仓库和 Pages 一并误报红色，新增 GitHub 公共 API 只读降级；Worker 仍保持真实红色，仓库与部署以黄色明确标注“GitHub 直连降级”。
- Pages 工作流 #31713769780 对提交 `1a02599` 构建成功。最终线上复核时 Worker 网络恢复，四张公共状态卡均为绿色：Worker/KV 正常、仓库 HEAD 为 `1a02599`、Pages 最近部署成功。
- 最终线上断点复核：1680×1050 主内容 760px、第三栏 280px且左右侧栏保留；390×844 为单列，右侧栏初始宽度 0，页面无横向溢出，搜索/主题独立工具行正常。

## 2026-08-14｜首页公共状态自检误报修复

- 复现 `workers.dev` 网络异常：DNS/链路不可达时请求约 8 秒超时，GitHub 公共 API 仍可正常读取。
- 新增 `src/lib/publicStatusRequest.ts` 与回归测试，区分网络错误、超时和 HTTP 服务错误；网络错误自动重试，避免短暂波动立即报红。
- 首页将 Worker `/health` 与 `/api/public-status` 分开检测；Worker 不可直连时显示黄色“当前网络无法直连 Worker”，仓库和 Pages 继续使用 GitHub 直连降级。
- 本地浏览器实际验证通过：管理台前端绿色，Worker 黄色，仓库与 Pages 黄色且显示 `ice11123/blog_test1`、HEAD 短 SHA 和部署记录；重复检测按钮在请求期间禁用。
- 质量门禁通过：`pnpm run check`、`pnpm run build`、`pnpm --dir worker test`（25/25）、`node --check worker/src/index.js`、`git diff --check`。

## 2026-08-14｜首页状态刷新提速

- 对比确认管理台 `/api/status` 使用 OAuth 直读 GitHub，首页 `/api/public-status` 使用匿名 KV 缓存；二者此前仅共享视觉组件，并非同一数据链路。
- 新增 stale 缓存和慢响应回归测试：旧缓存必须继续刷新 GitHub，Worker 超过 900ms 未响应时提前启动 GitHub 降级。
- 将 Worker 健康检查与仓库/Pages 检查解耦，卡片分别在各自请求完成时更新，不再互相阻塞。
- 本地浏览器计时验证：页面约 637ms 完成初始化，约 1477ms 显示最新仓库提交 `0558f25`；截图确认仓库和 Pages 不再停留于 `01fbfd1` 旧缓存。
- 提交 `bf2486b` 的 Pages 工作流 `31785120517` 构建成功；线上带缓存穿透参数复核约 3184ms 显示最新 SHA `bf2486b`，截图确认旧缓存已被 GitHub 直连结果替换。
# 2026-08-14｜首页语言、公告、状态与贡献墙优化

- 合并两个语言构成组件到单一外层面板，保留各自数据来源和图表渲染逻辑；桌面端使用主题色分隔线，移动端改为纵向堆叠。
- 增大公告栏标题、日期、正文和内部滚动区域，宽屏信息栏扩展到 360/440px；系统状态面板和卡片同步增大并保持 2×2 布局，539px 以下回落单列。
- GitHub 贡献墙新增英文眉标题、中文主标题和副说明，网格/图例在宽屏居中，移动端继续内部横向滚动。
- 质量门禁结果：`pnpm run check`、`pnpm run build`、`pnpm --dir worker test`、`node --check worker/src/index.js`、`git diff --check` 全部通过。
- 应用内浏览器当前禁止自动化访问本地 `127.0.0.1`，因此本轮先完成静态检查，推送 Pages 后再从公开 URL 做断点截图验收。
- 线上 `?v=5e03bf5` 复核确认语言区只有一个外框，1920/1680 为三栏，1440/1280 为两列，390/360 为单列；页面宽度分别为 1910/1670/1430/1270/381/350，均未产生横向溢出。
- 线上截图确认公告栏在固定滚动区域内、系统状态为 2×2（移动端单列）、贡献墙居中并横跨主体；Beads 任务 `blog_test1-hgh` 已关闭，完成记录提交为 `609907a`。
- 复核期间 Worker 健康卡为绿色；仓库和 Pages 卡偶发因 GitHub 公共 API 网络不可达显示红色，这是状态接口的真实结果，不影响静态页面构建。
