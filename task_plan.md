# 管理台与首页优化任务

## 目标

完成管理台说明、目录等高、实时预览和首页桌面双栏布局改造，并通过自动检查与实际浏览器检查。

## 阶段

- [x] 现状排查：确认预览占位渲染、管理台布局和首页模块结构。
- [x] 管理台结构与样式：加入操作说明，修复目录/编辑区等高。
- [x] 实时预览：实现 KaTeX、Mermaid、提示块、代码样式和安全 MDX 适配。
- [x] 首页布局：三组语义双栏，移动端单列。
- [x] 自动检查与浏览器验收：发现问题继续修复。

## 约束

- 不修改 OAuth、HttpOnly Cookie、CSRF、发布和删除链路。
- 不执行任意 MDX/JavaScript；预览仅在浏览器本地安全渲染。
- 不记录密码、Token、Secret。

## 错误记录

- 管理台首个补丁因乱码显示导致上下文不匹配；使用 UTF-8 读取确认真实文本后分段应用。
- 首轮类型检查失败：缺少 `@types/prismjs`，并且 `marked.Renderer` 返回类型写法不正确。
- 浏览器工具无法直接调整已有标签页视口；通过桌面实测与 CSS 断点/无溢出规则核对移动端回退。
