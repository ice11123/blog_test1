# 管理台与首页优化任务

## 目标

完成管理台说明、目录等高、实时预览和首页桌面双栏布局改造，并通过自动检查与实际浏览器检查。

## 阶段

- [x] 现状排查：确认预览占位渲染、管理台布局和首页模块结构。
- [x] 管理台结构与样式：加入操作说明，修复目录/编辑区等高。
- [x] 实时预览：实现 KaTeX、Mermaid、提示块、代码样式和安全 MDX 适配。
- [x] 首页布局：三组语义双栏，移动端单列。
- [x] 自动检查与浏览器验收：发现问题继续修复。
- [x] 首页视觉返工：恢复左右侧栏，重新设计模块对齐、字号与留白。
- [x] 对照原作者计算样式和截图进行桌面复验；移动端保留现有单列断点并完成无横向溢出检查。
- [x] 首页统一仪表盘重构：移除月度统计，将近期文章前移，状态与公告纳入统一网格，贡献墙横跨底部。
- [x] 首页视觉细化：欢迎统计居中、六卡等尺寸、状态文字换行、公告高度与语言模块对齐。
- [x] 自动检查、六档浏览器截图复核、维护日志同步与 Pages 部署验收。

## 约束

- 不修改 OAuth、HttpOnly Cookie、CSRF、发布和删除链路。
- 不执行任意 MDX/JavaScript；预览仅在浏览器本地安全渲染。
- 不记录密码、Token、Secret。

## 错误记录

- 首页状态组件首次补丁包含了不存在的上下文选择器，`apply_patch` 未应用任何内容；重新读取组件真实片段后使用精确锚点修复。
- 管理台首个补丁因乱码显示导致上下文不匹配；使用 UTF-8 读取确认真实文本后分段应用。
- 首轮类型检查失败：缺少 `@types/prismjs`，并且 `marked.Renderer` 返回类型写法不正确。
- 浏览器工具无法直接调整已有标签页视口；通过桌面实测与 CSS 断点/无溢出规则核对移动端回退。
- 上轮错误取舍：为了扩大首页内容宽度隐藏了左右侧栏，破坏了用户明确需要的三栏视觉骨架。
- 本轮环境偏差：初始工作目录不是 Git 根目录，实际仓库位于 `work/blog_test1`；已切换正确目录后继续。
- 收尾时曾误在仓库上层执行 `bd`/`git`，得到“no beads database found”和“not a git repository”；已改用真实仓库根目录，未产生任何提交或数据损坏。

## 追加阶段：迭代记录审计与流程固化

- [x] 对照最近 Git 提交、`progress.md`、`findings.md` 和 `src/content/maintenance.md`，找出未明确关联的记录。
- [x] 补写首页公共状态诊断、首页仪表盘重构、部署验收、Beads 状态和截图归档的提交链路。
- [x] 记录当前工作目录偏差，明确以后所有日志必须写入真实仓库根目录。
- [x] 固化每轮工作流程：开始前更新 `task_plan.md`，每次排查发现写入 `findings.md`，每个阶段结束写入 `progress.md`，完成后同步 `maintenance.md` 和提交哈希。

- 迁移后首次运行 `pnpm run check` 因跨盘移动造成 `node_modules` 不完整，pnpm 在无 TTY 下拒绝清理 modules；依赖目录属于未跟踪生成物，改为按 `pnpm-lock.yaml` 重新安装。
- 清理旧路径中仅剩的 `node_modules` 时，命令被执行环境的递归删除策略阻止；旧路径已不含 `.git` 或任何项目源码，不影响新仓库使用。

## 追加阶段：本地仓库目录迁移

- [x] 验证原仓库工作区干净、分支为 `main`、远程为 `ice11123/blog_test1`。
- [x] 将完整 `.git`、配置、源码、文档和构建资源迁移到 `D:\A Study files\AI_files\blog\blog_test1`。
- [x] 修复跨盘移动被深层 `node_modules` 路径打断造成的源码/`.git` 分离状态。
- [x] 按锁文件恢复依赖，并在新目录通过 `pnpm run check`。
- [x] 执行 `git fetch origin`，确认本地 `HEAD` 与 `origin/main` 一致。
