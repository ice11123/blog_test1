# 现状与排查结论

## 2026-08-14｜首页统一仪表盘重构现状

- 最终断点复核确认宽屏 1920/1680 为三列、中屏 1440/1280 为两列、390/360 为单列；状态卡无横向溢出，公告列表在固定区域内独立滚动，贡献墙位于最底部。
- 发现移动端右侧目录栏存在首屏 FOUC：首页服务端默认展开，而客户端稍后才执行收起。已改为移动端 CSS 默认收起，并用 `mobile-sidebar-open` 仅控制用户点击后的临时展开。
- 复核宽屏完整刷新时发现 `.fade-up-on-scroll` 只等待 `astro:page-load`，统计卡可能永久保持 `opacity: 0`；已补充首次 `DOMContentLoaded/readyState` 初始化，同时保留页面切换重建观察器。
- 由于部分浏览器在本地开发热刷新期间仍可能延迟 IntersectionObserver，首页统计卡再增加局部 `opacity: 1` 兜底，确保核心数字不会空白。
- 线上复核：Actions `31795982957` 对提交 `11d6850` 构建/部署成功；线上首页包含系统状态、维护公告、近期文章和底部贡献墙，不再渲染月度统计。线上公共 GitHub API 偶发不可达时，状态组件按既有降级策略显示异常，不影响 Pages 页面本身返回 200。
- 当前首页由 `.home-primary` 的三组双列模块与独立 `.home-info-column` 组成，右栏无法和左侧每一行共享网格轨道，因此公告下边界无法可靠对齐语言模块。
- 月度统计位于首行右侧，近期文章位于第三行右侧，贡献墙位于第三行左侧；要实现“近期文章前移、贡献墙置底、状态/公告逐行对齐”，需要把七个模块提升为同一个网格的直接子项。
- 状态卡的截断来自共享样式中的 `white-space: nowrap`、`text-overflow: ellipsis` 和 `overflow: hidden`；应只在首页组件中覆盖为可换行，避免改变管理台状态面板。
- 近期文章当前最多取 6 篇且复用完整 BlogCard；用户已确认改为最新 3 篇，并使用首页局部紧凑样式与“查看全部文章”入口。
- 用户确认 GitHub 贡献墙横跨整个首页主体并固定在所有信息模块之后；月度统计组件源码保留但首页不再导入或渲染。
- 首轮 1680×1050 浏览器复核确认统一网格的第一、二行边界完全对齐，状态卡所有字段均无横向溢出；同时发现公告列表在受限高度 Grid 中自动压缩轨道，折叠项只显示日期，需要使用 `grid-auto-rows: max-content` 与 `align-content: start` 保持条目高度并启用内部滚动。

## 2026-08-14｜首页状态与公告栏布局复核

- `src/components/home/PublicStatus.astro` 的 `.home-status-grid { grid-template-columns: 1fr; }` 覆盖了 `system-status.scss` 的公共两列设置，是状态卡纵向排列的直接根因。
- 1680×1050 下正文内容区可用宽度约 1093px，但 `.prose` 的 1em 内边距使 `.home-width-container` 只有约 1057px；直接增大右栏会压缩主内容。将宽屏 `.home-page` 设置为 `width: calc(100% + 2em); margin-left: -1em` 后，能够保持主内容 760px 并容纳 320px 信息栏。
- 1680×1050 实测状态/公告外框均为 x=1082.8、w=320；1920×1080 信息栏 w=380；1280×800 信息区下移，状态网格 w=639 且为两列；390×844 状态网格回落单列，文档无横向溢出。
- 现有状态数据链路未改动；本轮只调整首页布局与组件局部样式，Worker、缓存和 GitHub 降级逻辑保持不变。

- 当前提交为 `8d95aba`，工作区初始干净。
- 管理台 `src/scripts/admin-dashboard.ts` 使用 `marked`，会把公式、Mermaid 和所有大写 MDX 组件替换成占位文字；这是实时预览错误的根因。
- 正式站点使用 `remark-math`、`rehype-katex`、`remarkMermaid`、GitHub alerts 和 expressive-code。
- 正式 Mermaid 脚本只扫描全局 `pre.mermaid`，管理台预览需要独立的作用域渲染入口。
- Plot3D 使用 Plotly 和表达式执行，管理台预览不能复用正式组件，应显示安全适配卡。
- 首页当前最大宽度为 900px，模块是顺序纵向排列。
- 用户已确认采用三组语义配对：欢迎+统计/月份图，仓库语言/GitHub语言，贡献墙/近期文章。
- 用户要求后续维护过程持续写入博客仓库；本任务使用 planning-with-files 的 `task_plan.md`、`findings.md`、`progress.md`。
- 浏览器实际检查：KaTeX 已生成，Mermaid 已生成 SVG，Plot3D/Spoiler 安全适配存在，目录与编辑列/预览列高度一致。
- 实际检查发现代码块只剩 `}`；根因是去除 MDX import/export 的正则跨行吞掉了代码块中的 `export function`，必须改为只移除文档开头的 import/export 前缀。
- 首页首次实测在 1280px 下仍受左右博客侧栏挤压，中心仅约 741px、双栏单列约 326px；仅改 `.home-page` 最大宽度无法生效。决定为首页增加 `fullWidth` 布局，隐藏首页侧栏，文章页三栏保持不变。
- 用户明确否定隐藏首页侧栏，要求恢复左右侧栏；上轮 `fullWidth` 决策需要撤销。
- 原作者站点在 1280px 视口的实际基准：正文中心约 741px，左右侧栏各 220px，body 18px/30.6px，侧栏 13.6px/23.12px，正文 `.prose` padding 18px，主标题约 39.6px。
- 当前截图的主要审美问题不是单纯“内容宽”，而是把三个高度/信息密度完全不同的模块强行等分并排：顶部欢迎统计与空月图、贡献墙与文章卡形成不平衡空白；需要在 741px 中心栏内使用更紧凑的组合网格和统一卡片边界。
- 返工实现：恢复侧栏展开状态；中心首页最大 760px，使用 container query 在中心栏窄于 680px 时单列；卡片字号压到接近原作者正文基准，保留 18px body 基准，不对全站字体做破坏性改动。
- 首轮返工截图发现近期文章的 `BlogList` 内部嵌套 `main` 仍命中全局 `main { width: 720px }`，导致半栏文章卡溢出；首页局部重置嵌套 main 为 100%。
- 第二轮截图发现网格子项的 `height: 100%` 与 padding/border 叠加，导致面板高度超过网格行、上下边框相互顶住；改由 Grid 原生 stretch 等高，不再手工指定 100% 高度。
- 对照原作者线上首页再次测量：1280px 下正文 740.99px、左右侧栏各 220px、正文 18px/30.6px、侧栏 13.6px/23.12px；本地返工后的数值与其一致。
- 截图确认左右侧栏已存在且右侧目录能生成首页标题；截图缩放较小时侧栏容易被误判为消失，因此验收同时使用 DOM 盒模型和局部截图，不只依赖整页缩略图。
- 首页标题区保留原作者同款日期、站名和分隔线；本轮没有为了压缩页面再次删掉该结构，避免偏离“参考原作者字号与布局”的明确要求。
- Beads Dolt 同步两次均被 Windows 长路径拒绝；问题在 `.beads/embeddeddolt/.../git-remote-cache` 的内部缓存路径，不是网页代码或 GitHub Pages 构建问题。
- GitHub Pages 已成功部署新提交；无查询参数的旧标签页一度命中旧缓存，使用提交版本查询参数重新加载后得到新 DOM，确认 `full-width-layout` 已消失且两侧栏已恢复。
- 管理台公式错位的确定根因：KaTeX CSS 已加载，但最终 DOM 中所有 KaTeX 内联 `style` 均被 DOMPurify 的 `FORBID_ATTR: ['style']` 删除；积分上下限等节点因此丢失垂直定位。修复不能全局允许文章 style，应在普通内容净化完成后仅回填本地 KaTeX 的可信输出。
- Spoiler 与代码预览复现结论：Spoiler 正文实际存在但 `<details>` 默认关闭，导致实时预览看起来像内容丢失；Prism 已生成完整 `.token` 节点，但 `syntax-highlight.css` 只定义颜色变量、没有 token 选择器，所有 token 继承正文颜色。
- 管理台无限增高根因：`.admin-preview { height:100%; overflow-y:auto }` 的百分比高度来自同一 Grid 行，而 Grid 行又由 `.preview-body` 内容高度决定，形成“内容撑高行、行再撑高预览”的循环；中间 `.admin-form` 同时被 stretch 到 1380px，授权提示下方出现约 300px 空白。
- 修复后桌面端三列工作区固定为约 1460px，正文编辑框增至约 758px，授权提示距表单底部约 15px；长文压力测试将预览内容扩展到 11894px 时页面总高度仍保持 2318px，证明滚动被限制在 `.preview-body` 内部。
- 390×844 移动端实测：页面文档宽度 381px、无横向溢出；预览外框约 658px，内部内容约 1430px，并由预览正文独立滚动。
- 连续回车没有视觉反馈并非输入监听失效，而是 CommonMark/Marked 会把段落之间任意数量的空行折叠为同一个段落分隔；管理台预览需要在 Markdown 解析前提取额外空行并用安全占位节点恢复，但不能改写保存或发布的原始正文。
- 首页公共状态不能直接访问 GitHub 管理会话：新增匿名只读 Worker 接口 `/api/public-status`，仅返回仓库 `main` HEAD、部署状态和公开链接，并用 KV 缓存 90 秒；上游失败时仅允许旧缓存以 `stale: true` 降级。
- 生产构建发现 `new URL('../content/maintenance.md', import.meta.url)` 在 Astro 预渲染包内会解析到 `dist/.prerender/content` 并导致 ENOENT；维护公告必须用 `?raw` 在构建期导入。
- 1680px 且左右侧栏展开时，中央实际可用宽度约 1057px；采用 1040px 容器断点可容纳 760px 主内容与 280px 信息栏，1120px 以上再把信息栏扩至 340px。1440/1280 下保持原双列并把状态公告移到下方。
- 移动端会继承桌面端右侧栏展开偏好并覆盖正文。修复为移动端每次进入始终收起右侧栏，用户仍可用按钮临时展开；左侧抽屉改为 `transform` 完全移出视口，消除约 5px 横向溢出。
- 浏览器实测：1920/1680 显示第三信息栏和主题竖线；1440/1280 显示下方信息区和顶部横线；390/360 单列且右侧栏不再覆盖正文。搜索弹窗、主题菜单、亮绿主题刷新持久化和移动左侧抽屉均正常。
- 线上复核确认当前网络无法连接 `workers.dev`，但 GitHub API 可访问。公共状态前端增加只读降级：Worker 卡仍如实显示红色，仓库和 Pages 通过匿名 GitHub API 恢复展示并标记黄色“GitHub 直连降级”，不需要新增 Token。
- 2026-08-14 复核发现 `workers.dev` DNS/链路不可达：本机解析曾返回异常地址 `157.240.17.35`，连续请求约 8 秒 TCP 超时；GitHub 公共 API 可正常读取，因此没有证据表明 Worker、KV 或仓库服务端故障。
- 原首页把 Worker 网络/DNS 异常统一显示为红色“Worker 无法连接”，将网络不可达和服务故障混为一谈。本轮新增 `requestStatusJson()`，分开检测 `/health` 与 `/api/public-status`，网络/超时自动重试 3 次，网络不可达改为黄色等待态，明确 HTTP 4xx/5xx 才标为红色。
- Worker 状态失败时继续读取 GitHub 公共 API；仓库和 Pages 卡片标记“GitHub 直连降级”，不再因 Worker 链路受限而误报服务故障。
- 首页与管理台此前只复用了状态卡片样式，没有复用数据链路：管理台解锁后通过 OAuth 会话调用 `/api/status`，首页匿名调用带 KV 缓存的 `/api/public-status`。截图中的 `01fbfd1 · 数据暂未刷新` 证明首页收到 `stale: true` 后错误地将旧缓存作为最终结果。
- 首页原实现用 `Promise.all` 同时等待 `/health` 和 `/api/public-status`，导致快速健康检查也被较慢的仓库状态请求拖住；Worker 不可达时还要等三轮超时才开始 GitHub 降级。
- 修复后健康检查和仓库检查独立更新；仓库请求超过 900ms 或返回 stale/错误时立即启用 GitHub 公共 API。浏览器实测最新仓库 SHA 从页面加载到显示约 1477ms，不再停留于旧缓存。
# 2026-08-14｜首页语言、公告、状态与贡献墙优化复核

- 语言合并必须只增加一个外层 `language-overview-panel`；内部 `lang-card` 的背景、边框和最大宽度由首页局部规则清除，避免出现双重外框。
- `PublicStatus.astro` 原有共享状态网格被局部单列规则覆盖过，本轮明确恢复桌面 2×2，539px 以下才单列，并为标题、仓库路径、部署时间启用正常换行。
- 公告栏使用 `grid-auto-rows: max-content`、`align-content: start` 和内部滚动，避免折叠条目在受限网格轨道中被压缩到只剩日期。
- 贡献墙内部网格采用 `width: max-content` + `margin-inline: auto`；移动端取消居中以保留可滚动起点，避免整体页面横向溢出。
- 自动检查全部通过；待公开 Pages 部署完成后，继续以 1920/1680/1440/1280/390/360 视口截图确认真实布局。
